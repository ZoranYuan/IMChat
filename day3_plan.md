# Day 3: 消息可靠性和重连补数据

## 目标

把当前 IM 聊天链路里最容易出问题的部分收紧：

- 断线后消息不丢
- 重连后状态能补回来
- 广播失败有明确补偿思路
- 连接失败、投递失败、重连失败都有可观测性

---

## 当前关注点

当前主链路已经具备：

- 发消息
- 收消息
- 历史消息
- 已读确认
- 未读数
- WebSocket 实时同步

但消息可靠性还不够完整，重点问题是：

- 一次断线是否会导致消息状态错过
- 重连后是否会自动补离线消息
- 当前会话是否能恢复到正确上下文
- 广播失败是否有补偿入口

---

## 修改清单

### 修改 1：处理消息广播失败的补偿思路

**目标：** 落库成功后，优先保证 ACK；广播失败要可追踪。

**需要做的事：**

- 消息写库成功后，保证发送方能拿到 ACK
- 广播失败时记录日志
- 给后续补偿留入口

**当前原则：**

- ACK 优先于广播
- 广播失败不影响消息主流程
- 失败要有日志和可观测入口

---

### 修改 2：收敛 WebSocket 断线重连

**目标：** 断线后自动重连，重连后自动恢复聊天状态。

**需要做的事：**

- 断线后自动重连
- 重连成功后自动拉离线消息
- 当前会话自动补历史

**关注点：**

- 首次连接和重连要区分
- 重连成功后不能只恢复在线状态，必须补数据
- 当前会话在重连后要保持可继续阅读

---

### 修改 3：增加可观测性

**目标：** 连接问题、消息失败、重连失败都能看见。

**需要做的事：**

- 记录连接失败
- 记录重连失败
- 记录消息投递失败
- 给后续排障留证据

**建议原则：**

- 先让问题可见
- 再谈自动补偿
- 避免静默失败

---

## 验收标准

- 断线后能自动重连
- 重连后能自动拉离线消息
- 当前会话能恢复到正确状态
- 广播失败有日志
- 投递失败有记录
- 不会因为一次断连就丢消息状态

---

## 建议投入时间

- 1 小时：梳理断线重连流程
- 2 小时：补重连后的离线消息拉取
- 1 到 2 小时：记录失败日志和补偿入口
- 1 小时：模拟断网和恢复场景

---

## 实现顺序

1. 先确认重连触发点
2. 再补离线消息拉取
3. 然后补广播失败日志
4. 最后做断网恢复验证
