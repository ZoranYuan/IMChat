# 聊天业务完善计划

目标：把当前 IM 聊天链路从“可以发消息、可以看历史”补成“可以稳定展示、可以正确同步、可以面向实习项目讲清楚”的完整闭环。

## 5 天冲刺版

适用场景：

- 你每天能投入 5 到 6 小时
- 目标是在 `2026-06-15` 到 `2026-06-20` 这一轮面试前，把项目打磨到“能讲、能演示、能自证”的状态
- 原则不是继续扩功能数量，而是优先补“闭环、稳定性、展示效果”

### 5 天目标

- 聊天主链路稳定：发消息、收消息、历史消息、已读、未读、重连补数据
- 会话列表有产品感：搜索、排序、展示信息完整
- 群聊和一起看能联动：房间态能在聊天里看懂
- 项目能讲清楚：你知道每一块为什么做、还缺什么、怎么补

### 5 天排期

#### Day 1: 会话列表和搜索闭环

- 把会话列表搜索做成真正可用：[目前不做，后期引入 es 完成]
  - 按会话名过滤
  - 按最近消息过滤
  - 按好友名 / 房间名过滤
- 补会话卡片信息：
  - 单聊显示对方昵称优先
  - 群聊显示房间名
  - 最近消息区分文本 / 图片 / 视频 / 系统消息
- 统一会话排序：
  - 新消息自动置顶
  - 切换会话不丢当前状态
- 当天验收：
  - 你可以在面试里明确说清楚“会话列表怎么展示、怎么搜索、怎么排序”
- 建议投入时间：5 到 6 小时
  - 1 小时：梳理数据结构和过滤条件
  - 2 小时：实现前端搜索与展示
  - 1 到 2 小时：联调会话刷新和排序
  - 1 小时：补交互细节和自测

#### Day 2: 未读、已读、历史消息

- 收紧未读数计算规则：
  - 新消息进入会话时未读数 +1
  - 进入会话后清零
  - 当前会话不再错误累加
- 收紧已读回执：
  - 只在真正进入会话时发一次有效 ack
  - 避免重复进入时重复上报
  - 历史消息和实时消息统一使用 seq 逻辑
- 增加已读展示：
  - 消息列表能看出已读边界
  - 至少能说明“读到哪里了”
- 当天验收：
  - 你能解释未读数为什么不会乱跳
  - 你能解释已读回执是怎么闭环的
- 建议投入时间：5 到 6 小时
  - 1 小时：整理未读 / 已读规则
  - 2 小时：修正前后端 ack 和 seq 逻辑
  - 1 到 2 小时：补 UI 已读提示
  - 1 小时：验证重复进入、刷新、重连场景

#### Day 3: 消息可靠性和重连补数据

- 处理消息广播失败的补偿思路：
  - 落库成功后优先保证 ACK
  - 广播失败记录日志并进入补偿思路
- 收敛 WebSocket 断线重连：
  - 断线后自动重连
  - 重连后自动拉离线消息
  - 当前会话自动补历史
- 增加可观测性：
  - 记录连接失败、重连失败、消息投递失败
- 当天验收：
  - 你能说清楚“消息为什么不会因为一次断连就丢”
- 建议投入时间：5 到 6 小时
  - 1 小时：梳理断线重连流程
  - 2 小时：补重连后的离线消息拉取
  - 1 到 2 小时：记录失败日志和补偿入口
  - 1 小时：模拟断网和恢复场景

#### Day 4: 群聊和一起看联动

- 在群聊页展示房间态：
  - 当前房间名
  - 是否正在共享
  - 当前共享者
  - 当前视频信息
- 让聊天入口和一起看入口状态一致：
  - 没共享时可以发起
  - 别人共享时给出明确提示
- 保持聊天和 watch 切换上下文：
  - 从群聊进一起看不丢状态
  - 返回群聊时状态仍然正确
- 当天验收：
  - 你可以在演示里从聊天页自然过渡到一起看
- 建议投入时间：5 到 6 小时
  - 1 小时：梳理房间态需要暴露的字段
  - 2 小时：补聊天页房间状态展示
  - 1 到 2 小时：调整入口状态和文案
  - 1 小时：联调切换流程

#### Day 5: 工程收尾和面试材料

- 补核心测试：
  - 发消息
  - 已读回执
  - 历史分页
  - 未读更新
  - 重连补数据
- 收敛 TODO：
  - 把聊天主链路的 TODO 写成明确待办
  - 明确哪些是面试前完成，哪些是后续增强
- 补文档：
  - README 更新项目现状
  - update_plan.md 作为阶段性路线
  - 接口文档补聊天字段说明
- 做一次完整演示彩排：
  - 登录
  - 进群
  - 发消息
  - 看未读变化
  - 断线重连
  - 切到一起看
- 当天验收：
  - 你可以完整讲一遍项目闭环，不会卡壳
- 建议投入时间：5 到 6 小时
  - 2 小时：补主链路测试
  - 1 小时：收敛 TODO 和文档
  - 1 到 2 小时：做一次全流程演示彩排
  - 1 小时：整理面试问答卡片

### 面试前的交付标准

- 聊天主链路能稳定跑通
- 会话列表看起来像正式产品，而不是原型
- 已读 / 未读 / 历史 / 重连这几个面试高频点能讲明白
- 你能把“为什么优先做 agent，而不是再做一个一起看”说得有技术和产品依据

### 面试回答提纲

- 如果被问“你这个项目核心亮点是什么”
  - 重点说聊天闭环、未读已读、重连补数据、房间联动
- 如果被问“你做了什么难点”
  - 重点说消息序号、历史拉取、重复 ack、断线恢复、状态同步
- 如果被问“为什么不先做一个新的花哨功能”
  - 重点说先把 IM 地基打稳，再做 agent 这种高附加值功能更合理
- 如果被问“这个项目还能怎么继续扩展”
  - 重点说 agent 摘要、消息检索、会话知识库、智能提醒

### 面试前一天检查清单

- 项目能启动
- 登录可用
- 进群后能发消息
- 历史消息能拉出来
- 未读数和已读状态正常
- 断线后能自动恢复
- 群聊里能看到一起看状态
- README 和接口文档能打开

### 这 5 天不要再扩的内容

- 不要再加大而全的新模块
- 不要把一起看重构成另一个复杂项目
- 不要为了炫技去做不必要的富文本编辑器、复杂表情系统、完整消息撤回体系
- 不要把时间花在和面试讲述关系不大的细枝末节上

### 推荐时间安排

- `2026-06-04` 到 `2026-06-10` 之间完成 5 天游程最合适
- 这样你在 `2026-06-15` 前还有一段缓冲时间做：
  - 修 bug
  - 录演示
  - 准备项目介绍
  - 整理面试问答

---

当前判断：

- 核心收发消息链路已经能跑
- 会话历史、离线消息、WebSocket 实时同步、已读确认已经有基础实现
- 但从业务完整性来看，仍然有几个明显缺口，主要集中在：
  - 会话展示不完整
  - 未读/已读状态不够严谨
  - 消息同步和补偿机制还不够稳
  - 聊天页还缺少一些产品级能力

---

## 1. 会话列表能力不完整

### 现状

- 当前会话列表可以展示会话、未读数、最近一条消息
- 但是搜索框目前只是一个输入框，没有真正过滤会话
- 列表里缺少更强的上下文信息，比如：
  - 单聊对方身份展示是否稳定
  - 群聊是否有房间名 / 群名 / 成员信息
  - 最近消息类型的区分
  - 会话排序是否始终按最新活跃状态刷新

### 需要补的点

- 实现会话搜索：
  - 支持按会话名过滤
  - 支持按最近消息内容过滤
  - 支持按好友名 / 群名过滤
- 补全会话展示字段：
  - 单聊显示对方昵称优先，其次用户名
  - 群聊显示房间名
  - 最近消息需要区分文本、图片、视频、系统提示
- 会话排序规则统一：
  - 新消息到达后自动置顶
  - 打开会话后保持当前会话状态正确
  - 刷新离线消息后不打乱当前选中项

### 验收标准

- 输入关键字后会话列表实时缩小
- 会话卡片能明确看出它是单聊还是群聊
- 收到新消息时对应会话自动刷新到顶部
- 当前选中会话在刷新后不会丢失

---

## 2. 未读数和已读回执还不够严谨

### 现状

- 打开会话后会发送已读确认
- 后端会更新 `lastReadSeq`
- 但整个链路还存在一些不够完整的地方：
  - 历史消息首次加载时的已读边界还比较粗
  - 重新进入会话后是否重复发 ack 需要进一步收敛
  - 不同端同时在线时，未读数和已读状态的同步还不够强
  - 当前没有明显的“已读到哪一条”的 UI 展示

### 需要补的点

- 明确未读数计算规则：
  - 新消息进入会话时未读数 +1
  - 打开会话时当前会话未读清零
  - 当前会话内继续收到消息时，不再展示错误未读数
- 补全已读回执策略：
  - 只在真正进入会话后发送一次有效 ack
  - 同一会话重复进入时，避免无意义重复上报
  - 读取历史消息和实时消息时，统一使用同一套 seq 逻辑
- 增加已读提示 UI：
  - 在消息列表底部显示“已读到第几条”或“已读时间”
  - 如果暂时不做精确到用户的已读状态，至少让当前会话的已读状态可见

### 验收标准

- 未读数不会因为重复打开会话而乱跳
- 当前会话的未读数在进入后能稳定归零
- 重新刷新页面后，已读状态不会完全丢失
- 消息列表能看出已读边界

---

## 3. 消息补偿和可靠性还需要加强

### 现状

- 消息保存成功后会通过 MQ / WebSocket 广播
- 代码里已经有补偿相关的 TODO
- 但当前仍然存在几个风险：
  - 保存成功但消息广播失败时，前端可能短时间看不到
  - MQ 投递失败时缺少完整的 outbox 或重试闭环
  - WebSocket 短断连时，恢复逻辑主要依赖客户端重连

### 需要补的点

- 补一个最小消息可靠性闭环：
  - 消息落库成功后，先保证 ACK 正常返回
  - 广播失败时进入重试队列或补偿任务
  - 客户端重连后可通过历史接口补齐未收到的消息
- 增加投递失败可观测性：
  - 记录消息落库成功但广播失败的日志
  - 记录 MQ send 失败原因
  - 记录 websocket 断开和恢复次数
- 收敛重连后的补数据逻辑：
  - 重连后自动拉一次离线消息
  - 当前会话自动补拉历史记录

### 验收标准

- 单次 WebSocket 抖动不影响最终消息可见性
- MQ 短暂异常后消息不会永久丢失
- 前端重连后能自动补齐消息状态

---

## 4. 聊天输入与消息展示还偏基础

### 现状

- 目前支持文本消息发送
- 输入框、发送按钮、快捷换行都已经有基础
- 但还缺少产品级聊天体验：
  - 消息类型展示比较单一
  - 没有明确的发送失败重试入口
  - 没有输入中 / 正在发送的状态反馈
  - 没有消息引用、复制、删除等基础操作

### 需要补的点

- 补全消息状态展示：
  - 发送中
  - 发送成功
  - 发送失败
  - 重试发送
- 增加消息交互：
  - 复制文本
  - 长按 / 右键菜单
  - 至少支持删除自己发送的消息
- 增强富文本/多媒体类型展示：
  - 图片消息预览
  - 视频消息卡片
  - 系统消息样式

### 验收标准

- 用户能一眼看出消息发送是否成功
- 失败消息可以重试
- 视觉上能区分文本、图片、视频、系统消息

---

## 5. 聊天页和房间态还没有完全打通

### 现状

- 群聊已经和一起看房间关联起来了
- 但聊天页对房间态的表达还不够完整：
  - 房间是否正在共享
  - 当前共享者是谁
  - 当前视频是什么
  - 群聊和一起看入口的状态关系

### 需要补的点

- 在群聊头部补房间状态展示：
  - 当前房间名
  - 当前是否有人共享
  - 当前共享者
  - 当前播放视频信息
- 群聊输入区联动房间状态：
  - 没有共享时显示发起一起看入口
  - 当前别人共享时显示不可操作提示
- 让聊天和 watch 的状态切换更自然：
  - 从群聊进入一起看时保留上下文
  - 从一起看返回群聊时状态不丢失

### 验收标准

- 用户在群聊里能看懂当前房间正在发生什么
- 一起看状态不会和聊天状态脱节
- 房间态变化时聊天页会同步反映

---

## 6. 历史记录和回放能力还能再往前走一步

### 现状

- 已经有会话历史消息
- 已经有房间视频历史和弹幕回放
- 但这部分还偏“功能存在”，还没有完全产品化

### 需要补的点

- 历史消息分页体验：
  - 向上滚动加载更早历史
  - 保持滚动位置不跳动
- 回放入口统一：
  - 历史视频可直接回到对应房间上下文
  - 弹幕回放和消息历史能互相跳转
- 让历史数据更容易理解：
  - 视频标题
  - 发布时间
  - 消息数量
  - 最近互动时间

### 验收标准

- 历史消息可以连续翻页
- 回放入口能直接回到对应上下文
- 用户不需要猜“这条历史记录到底对应什么”

---

## 7. 工程层面还需要收尾

### 现状

- 项目已经有基础测试、接口文档和代码审查记录
- 但聊天业务仍然有一些 TODO 和潜在边界问题

### 需要补的点

- 为聊天主链路补测试：
  - 发送消息
  - 已读回执
  - 历史分页
  - 未读数更新
  - WebSocket 重连后的状态恢复
- 收敛 TODO 注释：
  - 对消息补偿的 TODO 形成明确待办
  - 对未读/已读边界形成明确实现方案
- 统一文档：
  - README 补聊天业务现状说明
  - OpenAPI 补接口字段说明
  - Apipost 集合补聊天场景

### 验收标准

- 聊天主链路至少有一套核心测试
- 文档能解释当前实现边界
- 代码里的 TODO 不再是模糊提醒，而是明确计划

---

## 推荐执行顺序

1. 先补会话搜索和会话展示
2. 再收紧未读 / 已读逻辑
3. 然后补消息可靠性和重连补数据
4. 最后补聊天页体验和历史回放体验

如果目标是实习项目展示，优先级建议是：

- 第一优先级：未读 / 已读 / 历史消息 / 重连补数据
- 第二优先级：会话搜索 / 会话展示 / 房间态联动
- 第三优先级：消息交互、富文本、多媒体和回放增强
